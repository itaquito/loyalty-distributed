apiVersion: v1
kind: Service
metadata:
  name: business-service
  namespace: loyalty-services
  labels:
    app: business-service
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: grpc
  selector:
    app: business-service
