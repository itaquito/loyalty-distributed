apiVersion: v1
kind: Service
metadata:
  name: business-service
  namespace: loyalty-services
  labels:
    app: business-service
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: business-service